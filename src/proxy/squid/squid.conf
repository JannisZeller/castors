# generated by castors; local edits will be overwritten

http_port 3128

# This proxy is an egress policy box, not a shared cache.
cache deny all
access_log stdio:/var/log/squid/access.log
cache_log stdio:/var/log/squid/cache.log
pid_filename /run/squid.pid

# Local manager access for container health checks.
acl manager proto cache_object
acl localhost src 127.0.0.1/32 ::1
http_access allow manager localhost
http_access deny manager

# Policy generated from castors config.
{{CASTORS_POLICY_RULES}}

http_access deny all
